# 阶段 7 第二优先级(3.3~3.7)项目优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `docs/learning-spring-boot/07` 的 3.3 可观测性、3.4 缓存、3.5 定时任务、3.6 API 文档、3.7 稳定性,落地成可运行的真实功能,并为每主题配一篇学习文档。

**Architecture:** 改动集中在 `com.rbac.common.*` 新增子包(observability / schedule / idempotent)与各模块少量接入点;依"能引库就引库"引入 Actuator、Spring Cache、springdoc、ShedLock(Redis provider)、Resilience4j;不做无关重构。

**Tech Stack:** Spring Boot 3.4.1 / Spring Security / data-redis(Lettuce) / MyBatis-Plus 3.5.9 / Micrometer / Actuator / Spring Cache + RedisCacheManager / springdoc-openapi 2.x / ShedLock / Resilience4j / JUnit 5 + Mockito + AssertJ。

## Global Constraints

- Java 21;Spring Boot 3.4.1(所有新依赖必须选 Spring Boot 3 / Jakarta EE 兼容版本)。
- context-path 为 `/api`;所有对外路径都在其下(actuator 实际为 `/api/actuator/**`,swagger 为 `/api/swagger-ui/index.html`)。
- API 契约:camelCase;枚举固定字符串;统一响应 `com.rbac.common.Result<T> {code,message,data}`;业务异常 `com.rbac.common.exception.BusinessException(int code, String messageKey, Object... args)`,文案走 i18n key(`i18n/messages*.properties`)。
- 单测风格:纯 JUnit 5 + Mockito + AssertJ,不启动 Spring 上下文(见 `InstanceServiceTest`)。配置/基建类改动用"构建 + 启动 + curl"验证。
- 安全红线:actuator 敏感端点需鉴权、swagger 生产可关;权限缓存不得影响"旧 Token 失效";幂等/限流后端强校验;日志/指标不带密码、Token、完整身份证。
- 新增配置项分环境放置(`application-{dev,test,prod}.yml`),公共默认值放 `application.yml`。
- 每主题实现完成后**立即**写对应学习文档 `docs/learning-spring-boot/{12..16}-*.md`,结构统一:为什么需要 → 本项目改了什么(文件+代码) → 原理讲解 → 对应 07 自测清单题 → 踩坑点。
- 每个 Task 结束需 `mvn -f backend/pom.xml -q -DskipTests package` 能通过(涉及单测的 Task 额外跑该测试类)。
- 提交信息结尾附:
  ```
  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01MsQVjZmkEohZ1JGY7GWajm
  ```

---

## 阶段 A · 3.3 可观测性与健康检查

### Task A1: 引入 Actuator 并暴露/保护端点

**Files:**
- Modify: `backend/pom.xml`(依赖区)
- Modify: `backend/src/main/resources/application.yml`(新增 `management` 段)
- Modify: `backend/src/main/resources/application-prod.yml`(端点收敛)
- Modify: `backend/src/main/java/com/rbac/security/config/SecurityConfig.java`(放行 health、保护其余端点)

**Interfaces:**
- Produces: `/api/actuator/health`(匿名可访问,含 readiness/liveness 组);`/api/actuator/{info,metrics,prometheus}`(需 `hasAuthority('monitor:view')`);新增权限码约定 `monitor:view`。

- [ ] **Step 1: 加依赖**

在 `backend/pom.xml` 的 `<dependencies>` 内(actuator 无需版本号,由 parent 管理):

```xml
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <!-- Prometheus 抓取端点(/actuator/prometheus) -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
            <scope>runtime</scope>
        </dependency>
```

- [ ] **Step 2: 公共配置暴露端点**

在 `backend/src/main/resources/application.yml` 末尾追加:

```yaml
# ── Actuator 可观测性端点(路径在 context-path 下,即 /api/actuator/**)──
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true          # 启用 liveness / readiness 探针组
      show-details: when-authorized   # 详情仅对已认证用户可见
  info:
    env:
      enabled: true
info:
  app:
    name: ${spring.application.name}
    description: RBAC 权限管理系统
```

- [ ] **Step 3: 生产环境收敛端点**

在 `backend/src/main/resources/application-prod.yml` 追加(生产不暴露 prometheus 明细给公网,只留 health/info):

```yaml
management:
  endpoints:
    web:
      exposure:
        include: health,info
```

- [ ] **Step 4: SecurityConfig 放行 health、保护其余端点**

修改 `SecurityConfig.java`:在 `WHITELIST` 数组加入 health 探针路径,并在 `authorizeHttpRequests` 中为其余 actuator 端点加权限要求。将 `authorizeHttpRequests` 段改为:

```java
                .authorizeHttpRequests(auth -> auth
                        .requestMatchers(WHITELIST).permitAll()
                        // actuator 健康探针匿名可访问(供容器 liveness/readiness 探测)
                        .requestMatchers("/actuator/health/**", "/actuator/health").permitAll()
                        // 其余管理端点需要监控查看权限,避免泄露配置/指标
                        .requestMatchers("/actuator/**").hasAuthority("monitor:view")
                        .anyRequest().authenticated())
```

> 注意:`requestMatchers` 路径相对 context-path,不带 `/api` 前缀。

- [ ] **Step 5: 构建并启动验证**

```bash
mvn -f backend/pom.xml -q -DskipTests package
# 需要 MySQL/Redis 已起(cd deploy && docker compose up -d)
mvn -f backend/pom.xml spring-boot:run
```

另开终端:
```bash
curl -s http://localhost:8080/api/actuator/health
# 期望:{"status":"UP","groups":["liveness","readiness"]}
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/api/actuator/metrics
# 期望:403(未带 Token / 无 monitor:view 权限)
```

- [ ] **Step 6: Commit**

```bash
git add backend/pom.xml backend/src/main/resources/application*.yml backend/src/main/java/com/rbac/security/config/SecurityConfig.java
git commit -m "feat(observability): 引入 Actuator,暴露 health/info/metrics 并保护敏感端点"
```

### Task A2: 业务指标埋点(审批 + 登录)

**Files:**
- Create: `backend/src/main/java/com/rbac/common/observability/WorkflowMetrics.java`
- Modify: `backend/src/main/java/com/rbac/workflow/task/service/TaskService.java`(审批/驳回埋点)
- Modify: `backend/src/main/java/com/rbac/auth/service/AuthService.java`(登录成功/失败计数;若失败在别处抛出,则接到 login 方法的 try/catch)
- Test: `backend/src/test/java/com/rbac/common/observability/WorkflowMetricsTest.java`

**Interfaces:**
- Produces: `WorkflowMetrics`,方法 `void recordApprove(boolean success, long millis)`、`Timer.Sample startSample()`;计数器名 `workflow.approve`(tag `result=success|fail`)、计时器 `workflow.approve.duration`;登录计数器 `auth.login`(tag `result=success|fail`)。

- [ ] **Step 1: 写失败测试**

`backend/src/test/java/com/rbac/common/observability/WorkflowMetricsTest.java`:

```java
package com.rbac.common.observability;

import io.micrometer.core.instrument.simple.SimpleMeterRegistry;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

class WorkflowMetricsTest {

    @Test
    void recordApproveIncrementsSuccessCounterAndTimer() {
        SimpleMeterRegistry registry = new SimpleMeterRegistry();
        WorkflowMetrics metrics = new WorkflowMetrics(registry);

        metrics.recordApprove(true, 12);

        assertThat(registry.get("workflow.approve").tag("result", "success").counter().count())
                .isEqualTo(1.0);
        assertThat(registry.get("workflow.approve.duration").timer().count())
                .isEqualTo(1L);
    }

    @Test
    void recordApproveIncrementsFailCounter() {
        SimpleMeterRegistry registry = new SimpleMeterRegistry();
        WorkflowMetrics metrics = new WorkflowMetrics(registry);

        metrics.recordApprove(false, 3);

        assertThat(registry.get("workflow.approve").tag("result", "fail").counter().count())
                .isEqualTo(1.0);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `mvn -f backend/pom.xml -q -Dtest=WorkflowMetricsTest test`
Expected: 编译失败(`WorkflowMetrics` 不存在)。

- [ ] **Step 3: 实现 WorkflowMetrics**

`backend/src/main/java/com/rbac/common/observability/WorkflowMetrics.java`:

```java
package com.rbac.common.observability;

import io.micrometer.core.instrument.MeterRegistry;
import io.micrometer.core.instrument.Timer;
import org.springframework.stereotype.Component;

import java.util.concurrent.TimeUnit;

/**
 * 工作流审批业务指标:成功/失败计数 + 审批耗时,供 Actuator/Prometheus 暴露。
 * 只记录聚合指标,不含任何敏感参数。
 */
@Component
public class WorkflowMetrics {

    private final MeterRegistry registry;

    public WorkflowMetrics(MeterRegistry registry) {
        this.registry = registry;
    }

    /** 计时用采样,在业务方法开始时创建。 */
    public Timer.Sample startSample() {
        return Timer.start(registry);
    }

    /** 记录一次审批结果与耗时(毫秒)。 */
    public void recordApprove(boolean success, long millis) {
        registry.counter("workflow.approve", "result", success ? "success" : "fail").increment();
        registry.timer("workflow.approve.duration").record(millis, TimeUnit.MILLISECONDS);
    }

    /** 记录一次登录结果。 */
    public void recordLogin(boolean success) {
        registry.counter("auth.login", "result", success ? "success" : "fail").increment();
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `mvn -f backend/pom.xml -q -Dtest=WorkflowMetricsTest test`
Expected: PASS(2 个测试)。

- [ ] **Step 5: 接入 TaskService 审批埋点**

修改 `TaskService.java`:注入 `WorkflowMetrics`(加入构造器),把 `approve` 包裹计时与计数:

```java
    public void approve(Long taskId, String comment) {
        long start = System.currentTimeMillis();
        boolean ok = false;
        try {
            workflowEngine.approve(taskId, comment);
            ok = true;
        } finally {
            metrics.recordApprove(ok, System.currentTimeMillis() - start);
        }
    }
```

> 构造器新增 `WorkflowMetrics metrics` 参数并赋值 `this.metrics = metrics;`,字段 `private final WorkflowMetrics metrics;`。`reject` 可同法记 `workflow.reject`,本任务只做 approve,reject 留文档说明。

- [ ] **Step 6: 接入登录计数**

修改 `AuthService.java` 的登录方法:成功路径调用 `metrics.recordLogin(true)`;认证失败(密码错/用户不存在抛异常处)调用 `metrics.recordLogin(false)` 后重新抛出。构造器注入 `WorkflowMetrics metrics`。若失败分支位于捕获不便处,用 try/catch 包住核心认证调用:

```java
        try {
            // ...原有认证与签发逻辑...
            metrics.recordLogin(true);
            return loginVO;
        } catch (RuntimeException e) {
            metrics.recordLogin(false);
            throw e;
        }
```

- [ ] **Step 7: 构建 + 单测**

Run: `mvn -f backend/pom.xml -q package`
Expected: BUILD SUCCESS(含 WorkflowMetricsTest)。

- [ ] **Step 8: Commit**

```bash
git add backend/src/main/java/com/rbac/common/observability/ backend/src/main/java/com/rbac/workflow/task/service/TaskService.java backend/src/main/java/com/rbac/auth/service/AuthService.java backend/src/test/java/com/rbac/common/observability/
git commit -m "feat(observability): 埋点审批成功/失败/耗时与登录计数"
```

### Task A3: 学习文档 12(可观测性)

**Files:**
- Create: `docs/learning-spring-boot/12-可观测性-Actuator与指标.md`

- [ ] **Step 1: 写文档**

按统一结构成文,至少覆盖:为什么只看控制台日志不够;本项目改了什么(A1/A2 的依赖、`management` 配置、`WorkflowMetrics`、SecurityConfig 端点保护,附关键片段);health vs liveness/readiness 区别;Micrometer 三种指标类型(Counter/Timer/Gauge)与本项目用法;`show-details=when-authorized` 与端点鉴权为何重要;对应 07 自测题"应用显示进程还活着是否代表能提供服务""一次审批耗时升高该看哪些指标";踩坑点(context-path 下 actuator 路径、prometheus 依赖 scope=runtime、生产端点收敛)。

- [ ] **Step 2: Commit**

```bash
git add docs/learning-spring-boot/12-可观测性-Actuator与指标.md
git commit -m "docs(learning): 12 可观测性 Actuator 与指标"
```

---

## 阶段 B · 3.4 缓存设计(Spring Cache 抽象)

### Task B1: 缓存管理器 + 权限缓存服务

**Files:**
- Modify: `backend/pom.xml`(加 `spring-boot-starter-cache`)
- Create: `backend/src/main/java/com/rbac/common/config/CacheConfig.java`
- Create: `backend/src/main/java/com/rbac/system/menu/service/PermissionCacheService.java`
- Modify: `backend/src/main/java/com/rbac/security/LoginUserAssembler.java`(改调缓存服务)
- Test: `backend/src/test/java/com/rbac/system/menu/PermissionCacheServiceTest.java`

**Interfaces:**
- Consumes: `SysMenuMapper.selectPermissionCodesByUserId(Long)`、`selectAllPermissionCodes()`(已存在)。
- Produces: `PermissionCacheService`,方法 `List<String> loadUserPermissions(Long userId)`(`@Cacheable(cacheNames="userPerms", key="#userId")`)、`List<String> loadAllPermissions()`(`@Cacheable("allPerms")`)、`void evictUser(Long userId)`、`void evictAll()`;cacheNames `userPerms`、`allPerms`。

- [ ] **Step 1: 加依赖**

`backend/pom.xml`:

```xml
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>
```

- [ ] **Step 2: 写失败测试(缓存命中:mapper 只查一次)**

`backend/src/test/java/com/rbac/system/menu/PermissionCacheServiceTest.java`——用真实 Spring Cache 代理验证 `@Cacheable`,以最小上下文启动:

```java
package com.rbac.system.menu;

import com.rbac.system.menu.mapper.SysMenuMapper;
import com.rbac.system.menu.service.PermissionCacheService;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.core.AutoConfigureCache;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.cache.annotation.EnableCaching;
import org.springframework.context.annotation.Import;

import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.times;
import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.when;

@SpringBootTest(classes = {PermissionCacheService.class, PermissionCacheServiceTest.CacheTestConfig.class})
@AutoConfigureCache   // 提供内存 ConcurrentMapCacheManager,免连 Redis
class PermissionCacheServiceTest {

    @EnableCaching
    static class CacheTestConfig { }

    @Autowired
    PermissionCacheService service;

    @MockBean
    SysMenuMapper menuMapper;

    @Test
    void userPermissionsAreCachedAfterFirstLoad() {
        when(menuMapper.selectPermissionCodesByUserId(9L)).thenReturn(List.of("user:list"));

        List<String> first = service.loadUserPermissions(9L);
        List<String> second = service.loadUserPermissions(9L);

        assertThat(first).containsExactly("user:list");
        assertThat(second).containsExactly("user:list");
        verify(menuMapper, times(1)).selectPermissionCodesByUserId(9L);  // 第二次走缓存
    }
}
```

- [ ] **Step 3: 运行确认失败**

Run: `mvn -f backend/pom.xml -q -Dtest=PermissionCacheServiceTest test`
Expected: 编译失败(`PermissionCacheService` 不存在)。

- [ ] **Step 4: 实现 PermissionCacheService**

`backend/src/main/java/com/rbac/system/menu/service/PermissionCacheService.java`:

```java
package com.rbac.system.menu.service;

import com.rbac.system.menu.mapper.SysMenuMapper;
import org.springframework.cache.annotation.CacheEvict;
import org.springframework.cache.annotation.Cacheable;
import org.springframework.stereotype.Service;

import java.util.List;

/**
 * 用户权限标识缓存(Cache Aside)。
 *
 * <p>权限集合读多写少,是天然缓存点:登录装配 {@code LoginUser} 时命中缓存,
 * 避免每次都联表查菜单。角色授权/用户角色变化时精准失效对应用户。
 *
 * <p>注意:此缓存只影响"新请求解析到的权限集",与"旧 Token 是否失效"正交——
 * Token 失效仍由 {@code TokenSessionService} 黑名单负责,二者互不替代。
 */
@Service
public class PermissionCacheService {

    private final SysMenuMapper menuMapper;

    public PermissionCacheService(SysMenuMapper menuMapper) {
        this.menuMapper = menuMapper;
    }

    /** 普通用户权限集合;缓存空集合以防穿透。 */
    @Cacheable(cacheNames = "userPerms", key = "#userId")
    public List<String> loadUserPermissions(Long userId) {
        return menuMapper.selectPermissionCodesByUserId(userId);
    }

    /** 超管全量权限。 */
    @Cacheable(cacheNames = "allPerms", key = "'ALL'")
    public List<String> loadAllPermissions() {
        return menuMapper.selectAllPermissionCodes();
    }

    /** 失效单个用户的权限缓存(用户改角色时调用)。 */
    @CacheEvict(cacheNames = "userPerms", key = "#userId")
    public void evictUser(Long userId) {
    }

    /** 失效全部权限缓存(角色改权限、影响面大时调用)。 */
    @CacheEvict(cacheNames = {"userPerms", "allPerms"}, allEntries = true)
    public void evictAll() {
    }
}
```

- [ ] **Step 5: 实现 CacheConfig(生产用 Redis + JSON + TTL 抖动)**

`backend/src/main/java/com/rbac/common/config/CacheConfig.java`:

```java
package com.rbac.common.config;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.cache.annotation.EnableCaching;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.cache.RedisCacheConfiguration;
import org.springframework.data.redis.cache.RedisCacheManager;
import org.springframework.data.redis.connection.RedisConnectionFactory;
import org.springframework.data.redis.serializer.GenericJackson2JsonRedisSerializer;
import org.springframework.data.redis.serializer.RedisSerializationContext;
import org.springframework.data.redis.serializer.StringRedisSerializer;

import java.time.Duration;
import java.util.Map;

/**
 * Spring Cache 抽象:统一 RedisCacheManager,key 前缀 rbac:cache:,value 存 JSON。
 * 各 cache 独立 TTL,并带随机抖动缓解雪崩。
 */
@Configuration
@EnableCaching
public class CacheConfig {

    @Bean
    public RedisCacheManager cacheManager(RedisConnectionFactory connectionFactory) {
        GenericJackson2JsonRedisSerializer json =
                new GenericJackson2JsonRedisSerializer(new ObjectMapper());

        RedisCacheConfiguration base = RedisCacheConfiguration.defaultCacheConfig()
                .prefixCacheNameWith("rbac:cache:")
                .serializeKeysWith(RedisSerializationContext.SerializationPair
                        .fromSerializer(new StringRedisSerializer()))
                .serializeValuesWith(RedisSerializationContext.SerializationPair
                        .fromSerializer(json));

        // 各 cache 的 TTL(带轻微差异,避免同一时刻集中过期)
        Map<String, RedisCacheConfiguration> configs = Map.of(
                "userPerms", base.entryTtl(Duration.ofMinutes(30)),
                "allPerms", base.entryTtl(Duration.ofMinutes(35))
        );

        return RedisCacheManager.builder(connectionFactory)
                .cacheDefaults(base.entryTtl(Duration.ofMinutes(10)))
                .withInitialCacheConfigurations(configs)
                .build();
    }
}
```

- [ ] **Step 6: LoginUserAssembler 改调缓存服务**

修改 `LoginUserAssembler.java`:构造器注入 `PermissionCacheService permissionCacheService`(替换直接依赖里权限查询用途,`menuMapper` 若仅用于权限可移除,否则保留)。把权限加载改为:

```java
        List<String> perms = isSuper
                ? permissionCacheService.loadAllPermissions()
                : permissionCacheService.loadUserPermissions(user.getId());
        Set<String> permissions = new HashSet<>(perms);
        loginUser.setPermissions(permissions);
```

> 构造器加参数 `PermissionCacheService permissionCacheService` 并赋值;若 `menuMapper` 只被这两处使用,则删除该字段与其构造参数;否则保留。

- [ ] **Step 7: 运行测试 + 构建**

Run: `mvn -f backend/pom.xml -q -Dtest=PermissionCacheServiceTest test && mvn -f backend/pom.xml -q package`
Expected: PASS + BUILD SUCCESS。

- [ ] **Step 8: Commit**

```bash
git add backend/pom.xml backend/src/main/java/com/rbac/common/config/CacheConfig.java backend/src/main/java/com/rbac/system/menu/service/PermissionCacheService.java backend/src/main/java/com/rbac/security/LoginUserAssembler.java backend/src/test/java/com/rbac/system/menu/PermissionCacheServiceTest.java
git commit -m "feat(cache): Spring Cache 抽象 + 用户权限集合缓存(Cache Aside)"
```

### Task B2: 授权变更时精准失效缓存

**Files:**
- Modify: `backend/src/main/java/com/rbac/system/role/service/RoleService.java`(`grantMenus`/`updateStatus`/`delete` 后失效)
- Modify: 用户改角色处(定位 `system/user/service` 中分配角色方法)
- Test: `backend/src/test/java/com/rbac/system/role/RoleServiceCacheEvictTest.java`

**Interfaces:**
- Consumes: `PermissionCacheService.evictUser(Long)`、`evictAll()`;`SysRoleMenuMapper`、角色-用户关联查询(定位现有 mapper 方法查某角色下 userIds;若无则用 `evictAll()` 兜底)。

- [ ] **Step 1: 定位角色下用户查询能力**

Run: `grep -rn "selectUserIdsByRoleId\|UserIdsByRole\|user_role" backend/src/main/java/com/rbac/system`
若存在按角色查 userIds 的 mapper 方法,`grantMenus` 后逐个 `evictUser`;若不存在,`grantMenus` 后调用 `evictAll()`(影响面可接受,文档说明取舍)。**本计划默认走 `evictAll()` 兜底**以避免新增 mapper。

- [ ] **Step 2: 写失败测试**

`backend/src/test/java/com/rbac/system/role/RoleServiceCacheEvictTest.java`(纯 Mockito,验证 grantMenus 后调用了 evictAll):

```java
package com.rbac.system.role;

import com.rbac.system.menu.service.PermissionCacheService;
import com.rbac.system.role.mapper.SysRoleMapper;
import com.rbac.system.role.mapper.SysRoleMenuMapper;
import com.rbac.system.role.service.RoleService;
import org.junit.jupiter.api.Test;

import java.util.List;

import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.verify;

class RoleServiceCacheEvictTest {

    @Test
    void grantMenusEvictsPermissionCache() {
        SysRoleMapper roleMapper = mock(SysRoleMapper.class);
        SysRoleMenuMapper roleMenuMapper = mock(SysRoleMenuMapper.class);
        PermissionCacheService cache = mock(PermissionCacheService.class);
        // 其余构造依赖按 RoleService 实际签名 mock 传入
        RoleService service = new RoleService(roleMapper, roleMenuMapper, /* 其余依赖 */ cache);

        service.grantMenus(1L, List.of(10L, 11L));

        verify(cache).evictAll();
    }
}
```

> 说明:`RoleService` 构造参数以实际为准(Step 3 会把 `PermissionCacheService` 加为最后一个参数);测试里其余依赖一并 `mock(...)` 传入。

- [ ] **Step 3: RoleService 注入缓存服务并在授权变更后失效**

修改 `RoleService.java`:构造器新增末位参数 `PermissionCacheService permissionCacheService`,字段 `private final PermissionCacheService permissionCacheService;`。在下列方法体末尾追加失效:
- `grantMenus(...)`:末尾 `permissionCacheService.evictAll();`
- `updateStatus(...)`(角色启停影响权限):末尾 `permissionCacheService.evictAll();`
- `delete(...)`(删角色):末尾 `permissionCacheService.evictAll();`

- [ ] **Step 4: 用户改角色处失效**

定位用户分配角色的 service 方法(`grep -rn "grantRoles\|assignRole\|user_role\|RoleIds" backend/src/main/java/com/rbac/system/user/service`)。在其成功修改角色后,对该用户调用 `permissionCacheService.evictUser(userId)`。构造器注入 `PermissionCacheService`。

- [ ] **Step 5: 运行测试 + 构建**

Run: `mvn -f backend/pom.xml -q -Dtest=RoleServiceCacheEvictTest test && mvn -f backend/pom.xml -q package`
Expected: PASS + BUILD SUCCESS。

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/java/com/rbac/system/role/service/RoleService.java backend/src/main/java/com/rbac/system/user/ backend/src/test/java/com/rbac/system/role/RoleServiceCacheEvictTest.java
git commit -m "feat(cache): 角色授权/用户角色变更后精准失效权限缓存"
```

### Task B3: 学习文档 13(缓存)

**Files:**
- Create: `docs/learning-spring-boot/13-缓存设计-SpringCache实战.md`

- [ ] **Step 1: 写文档**

覆盖:为什么要 Spring Cache 抽象(而非到处手写 Redis);本项目改了什么(CacheConfig、PermissionCacheService、LoginUserAssembler、授权变更失效,附片段);`@Cacheable/@CachePut/@CacheEvict` 语义;Cache Aside 模式;穿透(缓存空集合)/击穿/雪崩(TTL 抖动)/热 Key;为什么"权限缓存"与"Token 失效"正交(红线);`evictAll` vs 精准 `evictUser` 的取舍;对应 07 自测题"哪些权限数据可缓存,哪些必须实时";踩坑点(自调用导致 `@Cacheable` 失效、序列化选型、TTL 设定)。

- [ ] **Step 2: Commit**

```bash
git add docs/learning-spring-boot/13-缓存设计-SpringCache实战.md
git commit -m "docs(learning): 13 缓存设计 Spring Cache 实战"
```

---

## 阶段 C · 3.6 API 文档与兼容性

### Task C1: 引入 springdoc 并配置 OpenAPI + JWT 安全方案

**Files:**
- Modify: `backend/pom.xml`
- Create: `backend/src/main/java/com/rbac/common/config/OpenApiConfig.java`
- Modify: `backend/src/main/java/com/rbac/security/config/SecurityConfig.java`(放行文档路径)
- Modify: `backend/src/main/resources/application.yml`(springdoc 基础配置)
- Modify: `backend/src/main/resources/application-prod.yml`(生产关闭文档)

**Interfaces:**
- Produces: `/api/v3/api-docs`、`/api/swagger-ui/index.html`;OpenAPI 定义含 `bearerAuth`(HTTP bearer / JWT)安全方案与全局 `SecurityRequirement`。

- [ ] **Step 1: 加依赖**

`backend/pom.xml`(springdoc 2.7.x 适配 Spring Boot 3.4;固定版本):

```xml
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.7.0</version>
        </dependency>
```

- [ ] **Step 2: OpenApiConfig**

`backend/src/main/java/com/rbac/common/config/OpenApiConfig.java`:

```java
package com.rbac.common.config;

import io.swagger.v3.oas.models.Components;
import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.security.SecurityRequirement;
import io.swagger.v3.oas.models.security.SecurityScheme;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * OpenAPI 文档定义:标题/版本 + JWT(bearerAuth)安全方案。
 * 让接口契约可被工具验证,避免后端改字段导致前端静默出错。
 */
@Configuration
public class OpenApiConfig {

    private static final String BEARER = "bearerAuth";

    @Bean
    public OpenAPI rbacOpenAPI() {
        return new OpenAPI()
                .info(new Info()
                        .title("RBAC-Server API")
                        .description("RBAC 权限管理系统接口文档")
                        .version("v1"))
                .components(new Components().addSecuritySchemes(BEARER,
                        new SecurityScheme()
                                .type(SecurityScheme.Type.HTTP)
                                .scheme("bearer")
                                .bearerFormat("JWT")))
                .addSecurityItem(new SecurityRequirement().addList(BEARER));
    }
}
```

- [ ] **Step 3: springdoc 基础配置**

`backend/src/main/resources/application.yml` 追加:

```yaml
# ── OpenAPI / Swagger UI(访问 /api/swagger-ui/index.html)──
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
```

`backend/src/main/resources/application-prod.yml` 追加(生产默认不暴露文档):

```yaml
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
```

- [ ] **Step 4: SecurityConfig 放行文档路径**

在 `SecurityConfig.java` 的 `authorizeHttpRequests` 中,`anyRequest().authenticated()` 之前加入:

```java
                        .requestMatchers("/v3/api-docs/**", "/swagger-ui/**", "/swagger-ui.html").permitAll()
```

- [ ] **Step 5: 构建 + 启动验证**

```bash
mvn -f backend/pom.xml -q -DskipTests package
mvn -f backend/pom.xml spring-boot:run
```
另开终端:
```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/api/v3/api-docs
# 期望:200
```
浏览器打开 `http://localhost:8080/api/swagger-ui/index.html`,可见接口列表与右上角 Authorize(bearerAuth)。

- [ ] **Step 6: Commit**

```bash
git add backend/pom.xml backend/src/main/java/com/rbac/common/config/OpenApiConfig.java backend/src/main/java/com/rbac/security/config/SecurityConfig.java backend/src/main/resources/application*.yml
git commit -m "feat(openapi): 引入 springdoc,配置 JWT 安全方案,生产环境默认关闭文档"
```

### Task C2: 示范注解 + 学习文档 15(API 文档)

**Files:**
- Modify: `backend/src/main/java/com/rbac/auth/controller/AuthController.java`(加 `@Tag`/`@Operation`)
- Modify: 一个系统模块控制器(如 `system/role/controller/RoleController.java`)加注解示范
- Create: `docs/learning-spring-boot/15-API文档-OpenAPI.md`

- [ ] **Step 1: 给 AuthController 加注解**

在 `AuthController.java` 类上加 `@Tag(name = "认证", description = "登录/登出/刷新")`,`login` 方法上加 `@Operation(summary = "账号密码登录")`。导入 `io.swagger.v3.oas.annotations.Operation`、`io.swagger.v3.oas.annotations.tags.Tag`。

- [ ] **Step 2: 给一个系统控制器加注解**

在 `RoleController.java`(或就近的系统控制器)类上加 `@Tag(name = "角色管理")`,给分页/授权方法各加一个 `@Operation(summary = "...")` 作为示范。

- [ ] **Step 3: 构建验证**

Run: `mvn -f backend/pom.xml -q -DskipTests package`
Expected: BUILD SUCCESS。

- [ ] **Step 4: 写文档 15**

覆盖:OpenAPI 的价值是"契约可验证"而非只生成页面;本项目改了什么(依赖、OpenApiConfig、放行路径、注解示范、生产开关);JWT `bearerAuth` 如何在 UI 里 Authorize 后调用;接口版本/字段兼容/弃用策略要点;对应 07 自测题"后端改字段导致前端静默出错如何避免";踩坑点(context-path 下路径、生产关闭文档、`@Operation` 按模块补全)。

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/rbac/auth/controller/AuthController.java backend/src/main/java/com/rbac/system/role/controller/RoleController.java docs/learning-spring-boot/15-API文档-OpenAPI.md
git commit -m "docs(openapi): 控制器注解示范 + 15 API 文档学习笔记"
```

---

## 阶段 D · 3.5 定时任务与可靠后台任务

### Task D1: 引入 ShedLock(Redis provider)+ 定时基建

**Files:**
- Modify: `backend/pom.xml`
- Create: `backend/src/main/java/com/rbac/common/schedule/ScheduleConfig.java`
- Modify: `backend/src/main/java/com/rbac/common/config/RbacProperties.java`(新增 `workflow.overdueDays`)
- Modify: `backend/src/main/resources/application.yml`(新增 `rbac.workflow.overdue-days`)

**Interfaces:**
- Produces: `@EnableScheduling` + `@EnableSchedulerLock` 生效;`RbacProperties.getWorkflow().getOverdueDays()` 返回 `int`。

- [ ] **Step 1: 加依赖**

`backend/pom.xml`(ShedLock 5.x 兼容 Spring Boot 3):

```xml
        <dependency>
            <groupId>net.javacrumbs.shedlock</groupId>
            <artifactId>shedlock-spring</artifactId>
            <version>5.16.0</version>
        </dependency>
        <dependency>
            <groupId>net.javacrumbs.shedlock</groupId>
            <artifactId>shedlock-provider-redis-spring</artifactId>
            <version>5.16.0</version>
        </dependency>
```

- [ ] **Step 2: ScheduleConfig**

`backend/src/main/java/com/rbac/common/schedule/ScheduleConfig.java`:

```java
package com.rbac.common.schedule;

import net.javacrumbs.shedlock.core.LockProvider;
import net.javacrumbs.shedlock.provider.redis.spring.RedisLockProvider;
import net.javacrumbs.shedlock.spring.annotation.EnableSchedulerLock;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.connection.RedisConnectionFactory;
import org.springframework.scheduling.annotation.EnableScheduling;

/**
 * 定时任务基建:开启 @Scheduled,并用 ShedLock(Redis)保证多实例下同一任务只跑一个。
 * defaultLockAtMostFor 是兜底最长锁时长,防止实例崩溃后锁不释放。
 */
@Configuration
@EnableScheduling
@EnableSchedulerLock(defaultLockAtMostFor = "PT5M")
public class ScheduleConfig {

    @Bean
    public LockProvider lockProvider(RedisConnectionFactory connectionFactory) {
        return new RedisLockProvider(connectionFactory, "rbac");  // 锁 key 前缀 rbac
    }
}
```

- [ ] **Step 3: RbacProperties 新增 workflow 段**

在 `RbacProperties.java` 中新增内部类与 getter(与现有 `Jwt`/`Security` 同风格):

```java
    /** 工作流相关配置({@code rbac.workflow.*})。 */
    @Valid
    private final Workflow workflow = new Workflow();

    public Workflow getWorkflow() {
        return workflow;
    }

    /** {@code rbac.workflow.*} */
    public static class Workflow {

        /** 待办超时阈值(天),超过则被超时扫描任务标记。 */
        @Positive
        private int overdueDays = 3;

        public int getOverdueDays() {
            return overdueDays;
        }

        public void setOverdueDays(int overdueDays) {
            this.overdueDays = overdueDays;
        }
    }
```

- [ ] **Step 4: 配置默认值**

`backend/src/main/resources/application.yml` 的 `rbac:` 段下追加:

```yaml
  workflow:
    # 待办超时阈值(天)
    overdue-days: 3
```

- [ ] **Step 5: 构建验证**

Run: `mvn -f backend/pom.xml -q -DskipTests package`
Expected: BUILD SUCCESS。

- [ ] **Step 6: Commit**

```bash
git add backend/pom.xml backend/src/main/java/com/rbac/common/schedule/ScheduleConfig.java backend/src/main/java/com/rbac/common/config/RbacProperties.java backend/src/main/resources/application.yml
git commit -m "feat(schedule): 引入 ShedLock(Redis) 与定时任务基建"
```

### Task D2: 超时待办扫描任务(幂等)

**Files:**
- Create: `backend/src/main/java/com/rbac/workflow/task/schedule/OverdueTaskScanJob.java`
- Modify: `backend/src/main/java/com/rbac/workflow/task/mapper/WfProcessTaskMapper.java`(新增标记方法,若走 MyBatis-Plus 条件更新可免)
- Test: `backend/src/test/java/com/rbac/workflow/task/OverdueTaskScanJobTest.java`

**Interfaces:**
- Consumes: `WfProcessTaskMapper`(标记超时);`RbacProperties.getWorkflow().getOverdueDays()`。
- Produces: `OverdueTaskScanJob.scan()`,幂等——只把"未标记且创建时间早于阈值"的待办置 `overdue=true`(或写提醒日志),重复执行不产生重复副作用;返回本次标记条数 `int` 便于测试与日志。

- [ ] **Step 1: 确认待办实体字段**

Run: `grep -n "class WfProcessTask\|private\|createdAt\|status\|overdue" backend/src/main/java/com/rbac/workflow/task/entity/WfProcessTask.java`
- 若已有可用的"处理状态/创建时间"字段:扫描逻辑用 `status=PENDING` + `createdAt < 阈值`,标记方式选择:(a) 若实体可加 `overdue` 布尔列则走 Flyway `V3` 增列;(b) **默认更简单:不加列,任务只统计并 `log.warn` 记录超时待办数量与 id 列表**(避免 schema 变更),幂等天然成立(纯读+日志)。本计划采用 (b)。

- [ ] **Step 2: 写失败测试(纯 Mockito)**

`backend/src/test/java/com/rbac/workflow/task/OverdueTaskScanJobTest.java`:

```java
package com.rbac.workflow.task;

import com.rbac.common.config.RbacProperties;
import com.rbac.workflow.task.entity.WfProcessTask;
import com.rbac.workflow.task.mapper.WfProcessTaskMapper;
import com.rbac.workflow.task.schedule.OverdueTaskScanJob;
import org.junit.jupiter.api.Test;

import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

class OverdueTaskScanJobTest {

    @Test
    void scanReturnsCountOfOverduePendingTasks() {
        WfProcessTaskMapper taskMapper = mock(WfProcessTaskMapper.class);
        RbacProperties props = new RbacProperties();  // overdueDays 默认 3
        when(taskMapper.selectList(any())).thenReturn(List.of(new WfProcessTask(), new WfProcessTask()));

        OverdueTaskScanJob job = new OverdueTaskScanJob(taskMapper, props);
        int count = job.scan();

        assertThat(count).isEqualTo(2);
    }
}
```

- [ ] **Step 3: 运行确认失败**

Run: `mvn -f backend/pom.xml -q -Dtest=OverdueTaskScanJobTest test`
Expected: 编译失败(`OverdueTaskScanJob` 不存在)。

- [ ] **Step 4: 实现 OverdueTaskScanJob**

`backend/src/main/java/com/rbac/workflow/task/schedule/OverdueTaskScanJob.java`(字段名以实体实际为准,下例假设待办有 `status`/`createdAt`;如不同请对齐):

```java
package com.rbac.workflow.task.schedule;

import com.baomidou.mybatisplus.core.toolkit.Wrappers;
import com.rbac.common.config.RbacProperties;
import com.rbac.workflow.task.entity.WfProcessTask;
import com.rbac.workflow.task.mapper.WfProcessTaskMapper;
import net.javacrumbs.shedlock.spring.annotation.SchedulerLock;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

import java.time.LocalDateTime;
import java.util.List;

/**
 * 超时待办扫描:找出创建超过阈值天数仍处于待办(PENDING)的任务并记录告警。
 *
 * <p>幂等:纯读取 + 日志,重复执行不产生重复副作用。
 * <p>多实例:@SchedulerLock 保证同一时刻只有一个实例执行。
 */
@Component
public class OverdueTaskScanJob {

    private static final Logger log = LoggerFactory.getLogger(OverdueTaskScanJob.class);

    private final WfProcessTaskMapper taskMapper;
    private final RbacProperties props;

    public OverdueTaskScanJob(WfProcessTaskMapper taskMapper, RbacProperties props) {
        this.taskMapper = taskMapper;
        this.props = props;
    }

    /** 每天 02:00 执行。ShedLock 保证多实例只跑一次。 */
    @Scheduled(cron = "0 0 2 * * ?")
    @SchedulerLock(name = "overdueTaskScan", lockAtMostFor = "PT10M", lockAtLeastFor = "PT30S")
    public void run() {
        int count = scan();
        log.info("超时待办扫描完成,超时待办数量={}", count);
    }

    /** 返回超时待办条数(抽出便于单测)。 */
    public int scan() {
        LocalDateTime threshold = LocalDateTime.now().minusDays(props.getWorkflow().getOverdueDays());
        List<WfProcessTask> overdue = taskMapper.selectList(
                Wrappers.<WfProcessTask>lambdaQuery()
                        .eq(WfProcessTask::getStatus, "PENDING")
                        .lt(WfProcessTask::getCreatedAt, threshold));
        if (!overdue.isEmpty()) {
            log.warn("发现超时待办 {} 条,taskIds={}", overdue.size(),
                    overdue.stream().map(WfProcessTask::getId).toList());
        }
        return overdue.size();
    }
}
```

> 若实体字段名不是 `getStatus`/`getCreatedAt`,替换为实际 getter(Step 1 已确认)。待办"待处理"状态的枚举值以现有代码为准(可能是 `PENDING` 或其它)。

- [ ] **Step 5: 运行测试 + 构建**

Run: `mvn -f backend/pom.xml -q -Dtest=OverdueTaskScanJobTest test && mvn -f backend/pom.xml -q package`
Expected: PASS + BUILD SUCCESS。

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/java/com/rbac/workflow/task/schedule/OverdueTaskScanJob.java backend/src/test/java/com/rbac/workflow/task/OverdueTaskScanJobTest.java
git commit -m "feat(schedule): 超时待办扫描任务(幂等 + ShedLock 多实例互斥)"
```

### Task D3: 学习文档 14(定时任务)

**Files:**
- Create: `docs/learning-spring-boot/14-定时任务与分布式锁.md`

- [ ] **Step 1: 写文档**

覆盖:为什么需要后台任务;本项目改了什么(ShedLock 依赖、ScheduleConfig、OverdueTaskScanJob、overdueDays 配置,附片段);`@Scheduled` 固定频率 vs Cron;多实例为什么会重复执行;ShedLock 原理(锁记录 + `lockAtMostFor`/`lockAtLeastFor`,Redis provider 免建表);幂等设计(为何本任务纯读+日志天然幂等);验证多实例互斥的方法(本机起两个不同 `server.port` 实例观察只一个执行);何时升级到调度平台/MQ;对应 07 自测题"多实例定时任务重复执行如何避免";踩坑点。

- [ ] **Step 2: Commit**

```bash
git add docs/learning-spring-boot/14-定时任务与分布式锁.md
git commit -m "docs(learning): 14 定时任务与分布式锁"
```

---

## 阶段 E · 3.7 稳定性设计

### Task E1: 优雅停机 + 超时配置

**Files:**
- Modify: `backend/src/main/resources/application.yml`(graceful shutdown + Redis/Hikari 超时)

**Interfaces:**
- Produces: 应用支持优雅停机;Redis/数据库连接超时显式化。

- [ ] **Step 1: 配置优雅停机与超时**

`backend/src/main/resources/application.yml`:`server:` 段加 `shutdown: graceful`;新增 `spring.lifecycle.timeout-per-shutdown-phase`;显式化 Redis 与 Hikari 超时。追加/修改:

```yaml
server:
  port: 8080
  servlet:
    context-path: /api
  shutdown: graceful          # 优雅停机:停止接收新请求,放完在途请求

spring:
  lifecycle:
    timeout-per-shutdown-phase: 20s   # 等待在途任务/请求的最长时间
  data:
    redis:
      timeout: 2000ms          # 命令读取超时
      connect-timeout: 1000ms  # 建连超时
      lettuce:
        shutdown-timeout: 200ms
  datasource:
    hikari:
      connection-timeout: 3000   # 从连接池取连接的最长等待(毫秒)
      validation-timeout: 2000
```

> 注意:`spring` 段已存在(application/profiles/messages/flyway),这里是在同一 `spring:` 下补 `lifecycle`/`data.redis`/`datasource.hikari` 子键,勿重复顶层 `spring:`。

- [ ] **Step 2: 启动 + 优雅停机验证**

```bash
mvn -f backend/pom.xml -q -DskipTests package
mvn -f backend/pom.xml spring-boot:run
```
另开终端发送 `Ctrl-C`(或 `kill <pid>`),日志应出现 "Commencing graceful shutdown" / "Graceful shutdown complete",且不强杀在途请求。

- [ ] **Step 3: Commit**

```bash
git add backend/src/main/resources/application.yml
git commit -m "feat(stability): 优雅停机 + Redis/数据库连接超时显式化"
```

### Task E2: 幂等注解 + AOP(接工作流发起/审批)

**Files:**
- Create: `backend/src/main/java/com/rbac/common/idempotent/Idempotent.java`
- Create: `backend/src/main/java/com/rbac/common/idempotent/IdempotentAspect.java`
- Modify: `backend/src/main/java/com/rbac/workflow/instance/controller/InstanceController.java`(`start` 加注解)
- Modify: `backend/src/main/java/com/rbac/workflow/task/controller/TaskController.java`(`approve` 加注解)
- Modify: i18n `messages_zh_CN.properties` / `messages_en_US.properties` / `messages.properties`(错误文案 key)
- Test: `backend/src/test/java/com/rbac/common/idempotent/IdempotentAspectTest.java`

**Interfaces:**
- Consumes: `RedisTemplate<String,Object>`(SETNX 占位);`HttpServletRequest` 取幂等键请求头 `Idempotent-Key`;`BusinessException`。
- Produces: 注解 `@Idempotent`(属性 `int ttlSeconds() default 30`);切面命中重复键抛 `BusinessException("error.idempotent.duplicate")`;i18n key `error.idempotent.duplicate`。

- [ ] **Step 1: 加错误文案**

`messages_zh_CN.properties` 追加:`error.idempotent.duplicate=请勿重复提交`
`messages_en_US.properties` 追加:`error.idempotent.duplicate=Duplicate request, please retry later`
`messages.properties` 追加:`error.idempotent.duplicate=请勿重复提交`

- [ ] **Step 2: 定义注解**

`backend/src/main/java/com/rbac/common/idempotent/Idempotent.java`:

```java
package com.rbac.common.idempotent;

import java.lang.annotation.ElementType;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;

/**
 * 幂等控制:相同幂等键在 TTL 内只允许一次执行,重复请求被拒。
 * 客户端在请求头 Idempotent-Key 传唯一键(如前端生成的 UUID)。
 */
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface Idempotent {

    /** 幂等占位在 Redis 的存活秒数。 */
    int ttlSeconds() default 30;

    /** 请求头名。 */
    String header() default "Idempotent-Key";
}
```

- [ ] **Step 3: 写失败测试**

`backend/src/test/java/com/rbac/common/idempotent/IdempotentAspectTest.java`——验证首次放行、重复被拒(mock Redis 的 `setIfAbsent`):

```java
package com.rbac.common.idempotent;

import com.rbac.common.exception.BusinessException;
import org.aspectj.lang.ProceedingJoinPoint;
import org.junit.jupiter.api.Test;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.data.redis.core.ValueOperations;
import org.springframework.mock.web.MockHttpServletRequest;
import org.springframework.web.context.request.RequestContextHolder;
import org.springframework.web.context.request.ServletRequestAttributes;

import static org.assertj.core.api.Assertions.assertThatThrownBy;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.eq;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

class IdempotentAspectTest {

    @Test
    void duplicateKeyIsRejected() throws Throwable {
        RedisTemplate<String, Object> redis = mock(RedisTemplate.class);
        ValueOperations<String, Object> ops = mock(ValueOperations.class);
        when(redis.opsForValue()).thenReturn(ops);
        // 第二次 setIfAbsent 返回 false 表示键已存在
        when(ops.setIfAbsent(any(), any(), any())).thenReturn(false);

        MockHttpServletRequest req = new MockHttpServletRequest();
        req.addHeader("Idempotent-Key", "abc");
        RequestContextHolder.setRequestAttributes(new ServletRequestAttributes(req));

        IdempotentAspect aspect = new IdempotentAspect(redis);
        Idempotent ann = mock(Idempotent.class);
        when(ann.ttlSeconds()).thenReturn(30);
        when(ann.header()).thenReturn("Idempotent-Key");
        ProceedingJoinPoint pjp = mock(ProceedingJoinPoint.class);

        assertThatThrownBy(() -> aspect.around(pjp, ann))
                .isInstanceOf(BusinessException.class);

        RequestContextHolder.resetRequestAttributes();
    }
}
```

- [ ] **Step 4: 运行确认失败**

Run: `mvn -f backend/pom.xml -q -Dtest=IdempotentAspectTest test`
Expected: 编译失败(`IdempotentAspect` 不存在)。

- [ ] **Step 5: 实现切面**

`backend/src/main/java/com/rbac/common/idempotent/IdempotentAspect.java`:

```java
package com.rbac.common.idempotent;

import com.rbac.common.exception.BusinessException;
import org.aspectj.lang.ProceedingJoinPoint;
import org.aspectj.lang.annotation.Around;
import org.aspectj.lang.annotation.Aspect;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.stereotype.Component;
import org.springframework.util.StringUtils;
import org.springframework.web.context.request.RequestContextHolder;
import org.springframework.web.context.request.ServletRequestAttributes;

import jakarta.servlet.http.HttpServletRequest;
import java.time.Duration;

/**
 * 幂等切面:基于 Redis SETNX。命中重复幂等键则拒绝,防止重复提交(后端红线,而非仅前端禁用按钮)。
 */
@Aspect
@Component
public class IdempotentAspect {

    private static final String KEY_PREFIX = "idempotent:";

    private final RedisTemplate<String, Object> redisTemplate;

    public IdempotentAspect(RedisTemplate<String, Object> redisTemplate) {
        this.redisTemplate = redisTemplate;
    }

    @Around("@annotation(idempotent)")
    public Object around(ProceedingJoinPoint pjp, Idempotent idempotent) throws Throwable {
        HttpServletRequest request = currentRequest();
        String token = request != null ? request.getHeader(idempotent.header()) : null;
        if (!StringUtils.hasText(token)) {
            // 未带幂等键:要求客户端必须提供,避免默默放过重复请求
            throw new BusinessException("error.idempotent.duplicate");
        }
        Boolean first = redisTemplate.opsForValue()
                .setIfAbsent(KEY_PREFIX + token, "1", Duration.ofSeconds(idempotent.ttlSeconds()));
        if (Boolean.FALSE.equals(first)) {
            throw new BusinessException("error.idempotent.duplicate");
        }
        return pjp.proceed();
    }

    private HttpServletRequest currentRequest() {
        if (RequestContextHolder.getRequestAttributes() instanceof ServletRequestAttributes attrs) {
            return attrs.getRequest();
        }
        return null;
    }
}
```

- [ ] **Step 6: 接到发起流程与审批**

在 `InstanceController.java` 的 `start` 方法加 `@Idempotent`(import `com.rbac.common.idempotent.Idempotent`);在 `TaskController.java` 的 `approve` 方法加 `@Idempotent`。

- [ ] **Step 7: 运行测试 + 构建**

Run: `mvn -f backend/pom.xml -q -Dtest=IdempotentAspectTest test && mvn -f backend/pom.xml -q package`
Expected: PASS + BUILD SUCCESS。

- [ ] **Step 8: Commit**

```bash
git add backend/src/main/java/com/rbac/common/idempotent/ backend/src/main/java/com/rbac/workflow/instance/controller/InstanceController.java backend/src/main/java/com/rbac/workflow/task/controller/TaskController.java backend/src/main/resources/i18n/ backend/src/test/java/com/rbac/common/idempotent/
git commit -m "feat(stability): 幂等注解 + AOP(Redis SETNX),接入发起流程与审批"
```

### Task E3: 登录限流(Resilience4j)

**Files:**
- Modify: `backend/pom.xml`
- Modify: `backend/src/main/java/com/rbac/auth/controller/AuthController.java`(`login` 加 `@RateLimiter`)
- Modify: `backend/src/main/java/com/rbac/common/web/GlobalExceptionHandler.java`(处理 `RequestNotPermitted`)
- Modify: i18n(限流文案 key `error.rate.limited`)
- Modify: `backend/src/main/resources/application.yml`(ratelimiter 配置)

**Interfaces:**
- Produces: 登录接口受 `@RateLimiter(name="login")` 保护;超限抛 `RequestNotPermitted` → 统一 429 `Result`;i18n key `error.rate.limited`。

- [ ] **Step 1: 加依赖**

`backend/pom.xml`:

```xml
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot3</artifactId>
            <version>2.2.0</version>
        </dependency>
```

- [ ] **Step 2: 限流配置**

`backend/src/main/resources/application.yml` 追加:

```yaml
# ── Resilience4j 限流(登录接口防暴力破解)──
resilience4j:
  ratelimiter:
    instances:
      login:
        limit-for-period: 5          # 每个刷新周期允许的请求数
        limit-refresh-period: 10s    # 刷新周期
        timeout-duration: 0          # 拿不到令牌不等待,直接拒绝
```

- [ ] **Step 3: 加限流文案**

`messages_zh_CN.properties`:`error.rate.limited=操作过于频繁,请稍后再试`
`messages_en_US.properties`:`error.rate.limited=Too many requests, please try again later`
`messages.properties`:`error.rate.limited=操作过于频繁,请稍后再试`

- [ ] **Step 4: 给 login 加 @RateLimiter**

`AuthController.java` 的 `login` 方法加 `@RateLimiter(name = "login")`(import `io.github.resilience4j.ratelimiter.annotation.RateLimiter`)。

- [ ] **Step 5: GlobalExceptionHandler 处理超限**

在 `GlobalExceptionHandler.java` 新增处理器(429):

```java
    @ExceptionHandler(io.github.resilience4j.ratelimiter.RequestNotPermitted.class)
    @ResponseStatus(HttpStatus.TOO_MANY_REQUESTS)
    public Result<Void> handleRateLimited(io.github.resilience4j.ratelimiter.RequestNotPermitted e) {
        return Result.error(429, MessageUtils.get("error.rate.limited"));
    }
```

- [ ] **Step 6: 构建 + 启动验证**

```bash
mvn -f backend/pom.xml -q -DskipTests package
mvn -f backend/pom.xml spring-boot:run
```
另开终端快速连打登录接口(>5 次/10s):
```bash
for i in $(seq 1 8); do curl -s -o /dev/null -w "%{http_code} " -X POST http://localhost:8080/api/auth/login -H 'Content-Type: application/json' -d '{"username":"x","password":"y"}'; done; echo
# 期望:前若干次为 400/401(业务失败),达到阈值后出现 429
```

- [ ] **Step 7: Commit**

```bash
git add backend/pom.xml backend/src/main/java/com/rbac/auth/controller/AuthController.java backend/src/main/java/com/rbac/common/web/GlobalExceptionHandler.java backend/src/main/resources/i18n/ backend/src/main/resources/application.yml
git commit -m "feat(stability): 登录接口 Resilience4j 限流 + 统一 429 响应"
```

### Task E4: 学习文档 16(稳定性)

**Files:**
- Create: `docs/learning-spring-boot/16-稳定性设计-超时幂等限流优雅停机.md`

- [ ] **Step 1: 写文档**

覆盖:为什么需要稳定性设计;本项目改了什么(优雅停机与超时配置、幂等注解+AOP、限流,附片段);连接超时 vs 读取超时 vs 总超时;可重试 vs 会重复写入;幂等键/唯一约束/防重复提交(为什么前端禁用按钮不够,后端才是红线);限流/熔断/降级/隔离区别(熔断/重试本项目为何只讲原理不落代码——无不稳定外部依赖,YAGNI);优雅停机与在途事务/异步日志(呼应 3.2 线程池 `waitForTasksToCompleteOnShutdown`);对应 07 自测题"应用重启时异步日志和在途请求会不会丢";踩坑点。

- [ ] **Step 2: Commit**

```bash
git add docs/learning-spring-boot/16-稳定性设计-超时幂等限流优雅停机.md
git commit -m "docs(learning): 16 稳定性设计 超时/幂等/限流/优雅停机"
```

---

## 收尾 · 整体回归

### Task F1: 全量构建 + 冒烟

- [ ] **Step 1: 全量构建**

Run: `mvn -f backend/pom.xml clean package`
Expected: BUILD SUCCESS,全部单测通过。

- [ ] **Step 2: 冒烟(需 MySQL/Redis)**

```bash
cd deploy && docker compose up -d && cd -
mvn -f backend/pom.xml spring-boot:run
```
逐项对照 spec 第 5 节验收标准 1~7 快速核对:health/敏感端点 403、swagger 打开、限流 429、幂等重复被拒、优雅停机日志。

- [ ] **Step 3: 更新根 CLAUDE 记录(可选)**

若需要,补充 `docs/learning-spring-boot/07-...` 中 3.3~3.7 的"已完成"标注,或在学习文档索引处登记 12~16。

- [ ] **Step 4: Commit(如有改动)**

```bash
git add -A && git commit -m "chore: 阶段7第二优先级(3.3~3.7)收尾与文档登记"
```

---

## 自查记录(Self-Review)

- **Spec 覆盖**:3.3→阶段A;3.4→阶段B;3.5→阶段D;3.6→阶段C;3.7→阶段E;5 篇学习文档→A3/B3/C2/D3/E4。验收标准 1~8 → Task F1 逐项核对。
- **无占位**:所有代码步骤给出完整可编译代码;涉及"以实体实际字段为准"处均标注了先 `grep` 确认的前置步骤(B2 Step1、D2 Step1、B1 Step6)。
- **类型一致**:`PermissionCacheService` 方法名(loadUserPermissions/loadAllPermissions/evictUser/evictAll)、`WorkflowMetrics`(recordApprove/recordLogin/startSample)、`RbacProperties.Workflow.getOverdueDays()`、`@Idempotent(ttlSeconds/header)`、`IdempotentAspect.around(pjp, ann)` 在定义与调用处保持一致。
- **已知需实现期对齐的点**(非阻塞,已在步骤中标注):`RoleService` 构造参数顺序、用户改角色的 service 方法名、`WfProcessTask` 待办状态枚举值与时间字段 getter、`RoleController` 是否存在(不存在则换就近系统控制器)。
